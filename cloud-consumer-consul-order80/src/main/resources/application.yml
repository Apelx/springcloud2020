server:
  port: 80

spring:
  application:
    name: cloud-consumer-consul-order
  cloud:
    consul:
      host: 192.168.32.120
      #port: 8500
      port: 30007
      discovery:
        service-name: ${spring.application.name}
